# VaderConsulting.Config

C# .NET Framework 4.6 WinForms executable DR Planning Config (namespace `DR_Planning_Config`) with an empty `Form1`. The Visual Studio 2015 project (`Config.csproj`, ToolsVersion 14.0) is the stock WinExe template: `Program.Main` enables visual styles and runs `Form1`, whose designer only sets the window title. Assembly title/product is `DR Planning Config`; copyright 2015; company is blank. Settings and resources have no entries yet.

**Source last updated:** 2015-10-05 · **Language:** C# · **Target:** .NET Framework 4.6 · **Output:** WinForms executable (`WinExe`)

## Solution structure

| Project | Language | Type | Purpose |
|---------|----------|------|---------|
| `DR Planning Config` (`Config.csproj`) | C# | WinForms exe (`net46`) | Starter UI named DR Planning Config; `Form1` has no controls or logic yet. |

## How to open

Open `Config.csproj` in Visual Studio 2015 or later (ToolsVersion 14.0). There is no `.sln` in this folder.

## Requirements

- Visual Studio 2015 or later, .NET Framework 4.6

## Attribution and provenance

Working copy from my Historical Dev folder `VaderConsulting.Config`. Assembly title/product `DR Planning Config`; copyright `Copyright ©  2015`; company empty. Namespace `DR_Planning_Config`. Folder name on disk is `VaderConsulting.Config`.

## License

MIT © 2026 VaderConsulting. See `LICENSE`.
